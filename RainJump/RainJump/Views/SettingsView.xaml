<UserControl x:Class="RainJump.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource GameBgBlueBrush}">

    <Grid>
        <!-- Decorative bubbles -->
        <Canvas IsHitTestVisible="False">
            <Ellipse Canvas.Left="-60"  Canvas.Top="-60"    Width="200" Height="200" Fill="{DynamicResource BubbleWhiteBrush}"/>
            <Ellipse Canvas.Right="-40" Canvas.Bottom="-40" Width="220" Height="220" Fill="{DynamicResource BubbleWhiteBrush}"/>
        </Canvas>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <!-- Title -->
            <TextBlock Text="Settings"
                       FontFamily="{StaticResource TitleFont}"
                       FontSize="48"
                       FontWeight="Bold"
                       Foreground="{DynamicResource PinkAccentBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,50">
                <TextBlock.Effect>
                    <DropShadowEffect Color="{DynamicResource TitleShadowColor}"
                                      BlurRadius="8" ShadowDepth="3"
                                      Direction="270" Opacity="0.5"/>
                </TextBlock.Effect>
            </TextBlock>

            <Button x:Name="CustomizeButton" Content="Customize"
                    Style="{StaticResource MenuButtonStyle}"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,18"
                    Click="CustomizeButton_Click"/>

            <Button x:Name="BackButton" Content="Back"
                    Style="{StaticResource MenuButtonStyle}"
                    HorizontalAlignment="Center"
                    Click="BackButton_Click"/>

        </StackPanel>
    </Grid>
</UserControl>
